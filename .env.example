HOST=127.0.0.1
PORT=8787
DATABASE_PATH=data/channel.sqlite
MEDIA_DIR=data/videos

# Enables the protected moderation list and approve/reject endpoints.
ADMIN_TOKEN=replace-with-a-long-random-token

# The local deterministic provider is the safe default. To generate real
# videos, set VIDEO_PROVIDER=fal and FAL_KEY in the server environment only.
VIDEO_PROVIDER=mock
FAL_KEY=
FAL_MODEL=minimax/h3-max/text-to-video

GENERATION_CONCURRENCY=1
BUFFER_TARGET=4
WORKER_INTERVAL_MS=750
ROTATION_INTERVAL_MS=500
MOCK_GENERATION_DELAY_MS=800
PROVIDER_TIMEOUT_MS=900000

PROMPT_RATE_LIMIT=5
VOTE_RATE_LIMIT=30
LIKE_RATE_LIMIT=30
RATE_LIMIT_WINDOW_MS=60000
