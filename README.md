# Infinite Slop replica

A local, deterministic recreation of the canonical `infiniteslop.ai` landing experience. The layout, typography, responsive panel behavior, splash state, chat/queue controls, upvotes, likes, and local message flow are implemented without connecting to the original service's live or write APIs.

```bash
pnpm install
pnpm dev
```

Build a production bundle with `pnpm build`.
